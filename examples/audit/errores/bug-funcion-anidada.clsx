# BUG: funciones declaradas dentro de main: JIT falla con Trap sin contexto
function main(args: String[]) -> int {
    function suma(a: int, b: int) -> int { return a + b; };
    print("suma:", suma(3, 4));
    return 0;
};
