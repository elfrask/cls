interface Hello<T=Int> {
    num: T,
    greet(name: String): String,
};

interface Marcador<T> {
    real: T,
    fantasma: !T,
};

alias Vec3 = (Int, Int, Int);
alias Color = "red" | "green" | "blue";
alias FnInt = (Int) -> Int;
alias Dict = Record<String, Int>;

function id<T>(x: T) -> T {
    return x;
};

class Caja<T> {
    var contenido: T;
    function main(contenido: T) {
        me.contenido = contenido;
    }
    function obtener() -> T {
        return me.contenido;
    }
};

function main(args: String[]) -> int {
    var a: (Int, String) = (1, "x");
    var b: Vec3 = (1, 2, 3);
    var n: Hello["num"] = 1;
    var s: Hello<String>["num"] = "hola";
    var c: Color = "red";
    const k = "constante";
    var d: Dict = {a: 1};
    var idx = 0;
    var dyn = a[idx];
    var r: Marcador<String>["real"] = "ok";
    var f: Marcador<String>["fantasma"];
    var g: Int = id(5);
    var h: String = id("hola");
    var caja = Caja(42);
    var out: Int = caja.obtener();
    t = (
        <Div title="hola mundo" click={() -> { print("click") }}>
            <Dedo a="bb" />
        </Div>
    );

    
    return 0;
};