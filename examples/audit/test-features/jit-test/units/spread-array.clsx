# Spread en arrays (REST_SPREAD_PLAN): [0, ...a, 4].
function main(args: String[]) -> int {
    var a = [1, 2, 3];
    var b = [0, ...a, 4];
    print("b:", b);
    print("len:", len(b));
    var c = [...a, ...b];
    print("c:", c);
    print("c len:", len(c));
    return 0;
};
