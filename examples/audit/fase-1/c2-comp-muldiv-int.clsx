# C-2 isolate: compound *= /= con RHS int (confirmar mismo bug que mul)
function main(args: String[]) -> int {
    var f: float = 10.0;
    f *= 2;
    print("f *= 2 ->", f);
    f /= 4;
    print("f /= 4 ->", f);
    return 0;
};
