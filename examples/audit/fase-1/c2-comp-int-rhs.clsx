# C-2 isolate: compound += -= con RHS int, y %= **=
function main(args: String[]) -> int {
    var f: float = 10.0;
    f += 2;
    print("f += 2 ->", f);
    f -= 3;
    print("f -= 3 ->", f);
    f %= 3;
    print("f %= 3 ->", f);
    return 0;
};
