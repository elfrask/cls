# C-2 isolate: div compuesta y resto operaciones compuestas que si funcionan
function main(args: String[]) -> int {
    var f: float = 10.0;
    f += 2.5;
    print("f += 2.5 ->", f);
    f -= 1.5;
    print("f -= 1.5 ->", f);
    f /= 2;
    print("f /= 2 ->", f);
    return 0;
};
