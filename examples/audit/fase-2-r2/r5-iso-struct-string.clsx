# R5-iso: struct con campo String - escritura y lectura
structure Punto {
    x: int,
    nombre: String,
};
function main(args: String[]) -> int {
    var p = Punto(1, "a");
    print("antes:", p.x, p.nombre);
    p.nombre = "texto";
    print("despues:", p.x, p.nombre);
    return 0;
};
