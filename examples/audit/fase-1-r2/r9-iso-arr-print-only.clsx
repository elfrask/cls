import "json" as json;
function main(args: String[]) -> int {
    var a = json.parse('[1, 2, 3]');
    print("len:", len(a));
    var i = 0;
    while (i < len(a)) {
        print("a[", i, "] =", a[i]);
        i += 1;
    }
    return 0;
};
