# STRESS: bucle while infinito (debe ser matado por timeout)
function main(args: String[]) -> int {
    var i = 0;
    while (true) {
        i++;
        if (i % 1000000 == 0) {
            print("iter:", i);
        }
    }
    return 0;
};
