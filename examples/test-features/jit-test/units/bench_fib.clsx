function fib(n: int) -> int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
};

function main(args: String[]) -> int {
    var t = now();
    var r = fib(26);
    var d = now() - t;
    print("fib(26):", r);
    print("ms:", d);
    return 0;
};
