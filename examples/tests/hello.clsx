from "math" import math;

function main(args: String[]) -> int {
    print("CLS 2.0 - Hello!");
    print("Arguments:", args);
    for (i = 0; i < 10; i++) {
        print("number", i);
    };

    
    
    return 0;
}
