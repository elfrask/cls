[package]
name = "cls-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
wasm-encoder = { version = "0.255", optional = true }
wasmparser = { version = "0.255", optional = true }
cls-internals = { path = "../cls-internals", optional = true }

[features]
default = []
wasm-backend = ["dep:wasm-encoder", "dep:wasmparser", "dep:cls-internals"]

[dev-dependencies]
serde_json = "1"

[lib]
crate-type = ["rlib", "cdylib"]
