import "request" as req;

var valor:Array = [];

for each i (range(0, 100)) {
    valor.push({
        value:String(i)
    })
};

# [1, 2, 3, ..., 99]

print(valor.map((x) -> int { return int(x["value"]) } ))