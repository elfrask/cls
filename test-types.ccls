function add(a: int, b: int) -> int {
  return a + b
}

# Error: usando String como suma
function badAdd(a: int, b: str) -> int {
  return a + b
}

# Debería funcionar
function main(args) -> int {
  var x: int = 10
  var y: str = "hello"
  var z = add(x, 5)
  return 0
}
