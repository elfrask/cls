use crate::environment::Environment;
use crate::value::Value;

/// Registra funciones JSON
pub fn register(env: &mut Environment) {
    // TODO: implementar funciones nativas
    // env.define("parseJson", Value::Fun(...));
    // env.define("stringifyJson", Value::Fun(...));
}
